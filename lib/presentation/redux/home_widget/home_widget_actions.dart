/// Dispatched to force a re-render of the home-screen widgets (e.g. on app
/// resume so the "today" marker advances with the calendar day).
class RefreshHomeWidgetsAction {
  const RefreshHomeWidgetsAction();
}
